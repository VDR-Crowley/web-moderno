const games = {}

games.name = 'Halo'
games.genero = 'Ficcao Cientifica' 

games['codigo'] = 4523615463  // games['codigo de barra'] Evitar atributos com espaço

console.log(games)

// {} - é uma forma literal de criar um objeto
// Objeto é uma coleção de pares de chave e valor, os nomes dos objetos devem ser unicos
// no javascript vc pode ir definindo como o objeto vai se comportar dinamicamente 

// Anotação literal

const filmes = {
  name: 'Homem de ferro',
  desc: 'bla bla...'
}

console.log(filmes)
